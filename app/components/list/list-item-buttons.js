import styled from 'styled-components';

export const ListItemButtons = styled.div`
    flex: 0 1;
    display: flex;
    flex-direction: row;
    position: relative;
    -webkit-user-select: none;
`;